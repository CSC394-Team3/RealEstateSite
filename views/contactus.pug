doctype html

html
	head
	title Contact Us

	style.
		body {
			font-family: Arial, Helvetica, sans-serif;
			color: black;
			margin        : 0;
			padding       : 0;
			background-color: #cad297;
		}
	
	
	body 
		
		form(action="/contactus",method="post")
			div.input
				span.h1(style={color: 'black', 'font-size': '16px', 'tab-size': 24 }) <strong>Please type your message here: <strong>
				textarea(name="message", id="message")
			div.actions  
				input(type = "submit", name = "action", value = "done")

	

			